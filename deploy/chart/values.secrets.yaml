# sydega has no secrets. The static SPA reads no API keys, talks to no
# external services, has no DB. If that ever changes:
#   ./infra/scripts/app-secrets.sh edit sydega
# will replace this file with a SOPS-encrypted one.

app:
  secrets: {}
